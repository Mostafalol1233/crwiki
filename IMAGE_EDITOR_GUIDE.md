# دليل نظام معالجة الصور الاحترافي (Image Editor Guide)

تم تصميم هذا النظام ليوفر تجربة تعديل صور متكاملة واحترافية مباشرة داخل محرر النصوص (Rich Text Editor). يتيح النظام للمستخدمين تعديل الصور التي يتم رفعها أو الموجودة مسبقاً في المحتوى بسهولة.

## 🚀 كيفية الوصول إلى المحرر
ببساطة، قم بالنقر على أي صورة داخل محرر النصوص في صفحات (الأخبار، المقالات، أو الفعاليات)، ستظهر نافذة التعديل المنبثقة تلقائياً.

---

## 🛠 الوظائف والمميزات

### 1. الفلاتر والتأثيرات (Filters & Effects)
تتيح لك هذه الواجهة تحسين جودة الصورة بصرياً:
- **السطوع (Brightness):** التحكم في درجة إضاءة الصورة.
- **التباين (Contrast):** توضيح الفرق بين المناطق الفاتحة والداكنة.
- **التشبع (Saturation):** زيادة أو تقليل حيوية الألوان.
- **الأبيض والأسود (Grayscale):** تحويل الصورة إلى تدرجات الرمادي.
- **التعتيق (Sepia):** إعطاء الصورة مظهراً كلاسيكياً قديماً.
- **التغبيش (Blur):** إضافة تأثير الضبابية على الصورة.
- **عكس الألوان (Invert):** عكس ألوان الصورة (تأثير النيجاتيف).
- **تدوير الألوان (Hue Rotate):** تغيير درجات الألوان في الصورة.
- **التدوير (Rotate):** تدوير الصورة بزاوية 90 درجة في كل نقرة.

### 2. قص الصور (Cropping)
أداة تفاعلية لاقتطاع أجزاء محددة من الصورة:
- **التفعيل:** قم بتفعيل خيار "Enable Cropping".
- **التحكم:** اسحب منطقة التحديد أو استخدم المقابض الثمانية (Handles) لتحديد المنطقة بدقة.
- **القوالب الجاهزة:** استخدم أزرار (1:1 Square) أو (16:9) للقص بنسب أبعاد ثابتة وشائعة.
- **التطبيق:** انقر على "Apply Crop" لاعتماد القص قبل الحفظ النهائي.

### 3. تغيير الحجم (Resizing)
تحكم كامل في أبعاد الصورة:
- **السحب المباشر:** يمكنك تغيير حجم الصورة بسحب المقابض الزرقاء الموجودة على حواف الصورة في المعاينة.
- **الإدخال الرقمي:** أدخل الأبعاد المطلوبة (الطول والعرض) بالبكسل يدوياً.
- **نسبة الطول للعرض:** خيار "Maintain aspect ratio" يضمن عدم تشوه الصورة أثناء تغيير حجمها.
- **الاختصارات:** أزرار سريعة لتغيير الحجم (50%, 75%, 150%, 200%).

### 4. الأنماط والحواف (Styles & Borders)
تخصيص شكل إطار الصورة:
- **الشكل (Shape):** اختر بين الشكل المستطيل (Rectangle) أو الدائري (Circle).
- **تدوير الحواف (Border Radius):** جعل زوايا الصورة دائرية بنسب متفاوتة.
- **الإطارات (Borders):** التحكم في سمك الإطار ولونه باستخدام منتقي الألوان (Color Picker).

---

## ⚙️ التخصيص التقني (Developers Guide)

المكون الأساسي هو `ImageEditorModal.tsx` ويمكن تخصيصه من خلال المعاملات التالية:

### معاملات التهيئة (Props)
- `isOpen`: التحكم في ظهور النافذة.
- `imageSrc`: رابط الصورة المراد تعديلها.
- `onSave`: دالة يتم استدعاؤها عند الحفظ، تعيد الرابط الجديد (URL أو Base64).
- `onClose`: دالة لإغلاق النافذة.
- `config`: كائن اختياري للتحكم في الإعدادات:
    - `maxSizeMB`: أقصى حجم للملف (افتراضياً 1MB).
    - `maxWidthOrHeight`: أقصى أبعاد للصورة (افتراضياً 1920px).
    - `allowedTypes`: أنواع الملفات المسموحة.
    - `quality`: جودة الصورة المصدرة (0.1 إلى 1.0).

### معايير الأداء والجودة
- **الضغط (Compression):** يتم ضغط الصور تلقائياً قبل الرفع لضمان سرعة تحميل الصفحات (الحد الأقصى 1MB افتراضياً).
- **صيغ الملفات:** يدعم النظام JPEG, PNG, WebP, GIF.
- **الحفظ الذكي:** يحاول النظام رفع الصورة للسيرفر أولاً، وفي حال فشل الاتصال يتم حفظها بتنسيق Base64 داخل المحتوى لضمان عدم فقدان التعديلات.

---

## 📱 التوافقية
تم اختبار النظام ليعمل بكفاءة على:
- متصفحات الكمبيوتر (Chrome, Firefox, Safari, Edge).
- الأجهزة اللوحية والهواتف الذكية (واجهة مستجيبة بالكامل).

---
*ملاحظة: للحصول على أفضل جودة عند استخدام الحواف الدائرية أو الشكل الدائري، سيقوم النظام تلقائياً بحفظ الصورة بصيغة PNG لدعم الشفافية.*
