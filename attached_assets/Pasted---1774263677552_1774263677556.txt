اكمل هذه التاسك
وكمان بعد ما تخلص خش علي المحتوي
ازاي اول حاجه هتصلح اخر ايفينتين تمت اضافتهم هتعدل الصوره بتاعتهم اليخاريجه عشان هيا بايظه والي ظاهر ده فول باك
2- هتظبط صور الرانك
3-هتخليني اعدل الاربع اسلحه الي بيظهرو في الصفحه الرئيسيه اقدر اتحكم بيهم عادي في لوحه الادمن واختار اي سلاح عشان يظهر تمام
بس قبل كل ده تاكد ان الداتا بايس متصله

المشكلة واضحة - الـ MongoDB مش متصلة بسبب الـ connection string. خليني أفحص كل حاجة الأول.

الـ MONGODB_URI موجود كـ secret لكن قيمته غلطة. خليني أفحص كيف بيتقرأ في الكود.

المشكلة واضحة - الـ MONGODB_URI موجود لكن قيمته غلطة (مش بتبدأ بـ mongodb:// أو mongodb+srv://). محتاج تدي لي الرابط الصح. خليني أطلبه منك:

محتاج الـ MongoDB connection string الصح عشان قاعدة البيانات تشتغل. المفروض يبدأ بـ mongodb:// أو mongodb+srv:// - هتلاقيه في MongoDB Atlas أو الخدمة اللي بتستخدمها.

1 secret requested
•
MONGODB_URI
ممتاز! MongoDB اتصلت بنجاح. دلوقتي خليني أفحص الكود عشان أشتغل على الثلاث مهام.

الاتصال شغال. دلوقتي خليني أفحص المشاكل الثلاث.

الآن فاهم المشاكل. خليني أفحص بيانات الرتب وكود الأدمن.

الآن أنا عارف كل حاجة. خليني أشوف كيف بيتم الـ reseed للرتب، وأبدأ أشتغل على المهام الثلاث بالتوازي.

خطة العمل:

إصلاح صور الرتب - تحديث بيانات الرتب بالـ URLs الصح من seed
إصلاح صور آخر 2 Events
إضافة تحكم في الأسلحة المميزة من لوحة الأدمن
الـ reseed بدأ يشتغل. خليني أتحقق من الوضع الحالي للرتب والـ api/posts error.

وجدت المشكلة! الـ RankSchema فيه imageUrl بس الـ seed data بيستخدم image وemblem. خليني أصلح كل حاجة الآن.

الآن أبدأ بتطبيق كل الإصلاحات. هشتغل على كل حاجة بالتوازي.

المشاكل التي وجدتها:

RankSchema عنده imageUrl بس الـ seed بيستخدم image و emblem → مش بيتحفظ
/api/posts بيرمي error لأن result.items ممكن يبقى undefined
آخر 2 events عندهم صور akamaized.net محمية من hotlink
الأسلحة المميزة مش موجودة في لوحة الأدمن
دلوقتي أبدأ:

الآن أنفذ كل التغييرات. هبدأ بالتعديلات الأساسية.

1. إصلاح RankSchema - إضافة image و emblem:

2. إضافة featuredWeapons إلى SiteSettingsSchema:

3. إضافة featuredWeapons إلى Zod schema:

4. إصلاح Posts API error:

5. الآن أضيف endpoint للأسلحة المميزة وأصلح صور الـ Events:

الآن أضيف الـ public endpoint للأسلحة المميزة وأصلح صور الـ events:

الآن دعني أضيف endpoint للحصول على أسلحة بـ IDs محددة:

الآن أصلح صور الـ events وأحدث لوحة الأدمن والصفحة الرئيسية في نفس الوقت:

الآن أحدث Admin.tsx - أضيف featuredWeapons للـ state والـ UI:

يوجد تكرار. دعني أصلحه:

الآن أضيف featuredWeapons إلى الـ useEffect:

الآن أضيف واجهة الأسلحة المميزة في لوحة الأدمن - سأضيفها قبل Quick page preview:

الآن أحدث Home.tsx لاستخدام الأسلحة المميزة:

الآن أستبدل recentWeapons بـ displayWeapons في عرض الأسلحة:

أضيف onError handlers لصور الـ events وأصلح البيانات في قاعدة البيانات:

لازم أصلح ترتيب الـ routes! الـ /api/weapons/batch/by-ids يجب أن يكون قبل /api/weapons/:id:

الآن أكتب script لإصلاح صور الـ events مباشرة في MongoDB، وأعيد تشغيل التطبيق:

الـ build نجح! الآن أشغل سكريبت إصلاح قاعدة البيانات:

ممتاز! 47 رتبة اتحدثت وآخر 2 events اتصلحت. الآن أعيد تشغيل الخادم لتطبيق التغييرات: