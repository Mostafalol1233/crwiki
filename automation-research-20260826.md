
## مصادر الجمع التي تم التحقق منها

صفحة الأسلحة في CrossFire Fandom هي مصدر واسع يضم آلاف الصفحات وروابط أقسام وقوائم، وتعرض نصوصًا وصورًا وروابط صفحات فرعية؛ لذلك لا يكفي أخذ العنوان والصورة المصغرة فقط. المصدر المحدد الذي تمت مراجعته هو `https://crossfirefps.fandom.com/wiki/Weapons`.

يوضح توثيق MediaWiki الرسمي أن واجهة Parse تستطيع إرجاع محتوى الصفحة بصيغة HTML المحللة أو Wikitext الخام، وأن Revisions تستطيع إرجاع Wikitext، مع إمكانية تحديد الصفحة أو revision. هذا يسمح ببناء جمع قابل للتتبع يعتمد على API مع HTML كخطة مساندة، بدل محاولة النقر على أزرار الواجهة أو ادعاء تنفيذ JavaScript داخل Vercel. المصدر هو `https://www.mediawiki.org/wiki/API:Get_the_contents_of_a_page`.

الملاحظة التشغيلية: صفحات Fandom قد تحتوي أقسامًا وروابط داخلية وصورًا وفيديوهات، لكن جمعها كاملًا يتطلب حدودًا للمعدل، قائمة سماح للمجال، إزالة الإعلانات والعناصر غير التحريرية، حفظ المصدر ووقت الالتقاط، ووضع النتائج في مسودات قبل النشر. لم تُكتب أي بيانات من المصدر إلى Supabase في هذه المرحلة.

## توثيق التشغيل التلقائي والكشط

يوضح توثيق Vercel الرسمي أن وظائف cron تُعرّف في `vercel.json`، وتستدعي المسار بطلب HTTP، وأنها تعمل على نشر Production لا Preview. كما يرسل Vercel قيمة `CRON_SECRET` في رأس Authorization عند ضبطها، ويجب حماية المسار بمقارنة Bearer مع المتغير الخادمي. على الخطة المجانية يكون التشغيل مرة واحدة يوميًا، والتوقيت UTC، ويجب أن تكون العملية قابلة لإعادة التشغيل دون تكرار ضار. المصدران: `https://vercel.com/docs/cron-jobs` و`https://vercel.com/docs/cron-jobs/manage-cron-jobs` و`https://vercel.com/docs/cron-jobs/quickstart`.

يوضح توثيق Firecrawl الحالي أن `POST /v2/crawl` يعيد معرّف مهمة غير متزامنة، وأن النتائج تُستعاد بالاستعلام عن حالة المهمة. يدعم crawl حدًا للصفحات، وعمق الاكتشاف، وتضمين أو استبعاد المسارات، واستخدام sitemap، وخيارات scraping لكل صفحة. تكلفة كل صفحة تُحسب، والحد الافتراضي كبير؛ لذلك يجب ضبط حد صغير ومعدل تشغيل محافظ. النتائج متاحة لفترة محدودة، ويجب التعامل مع التكرار والأخطاء. المصادر: `https://docs.firecrawl.dev/features/crawl` و`https://docs.firecrawl.dev/api-reference/endpoint/crawl-post` و`https://docs.firecrawl.dev/api-reference/endpoint/crawl-get`.

اختبار Firecrawl على `https://crossfirefps.fandom.com/wiki/Weapons` أعاد تقريبًا 42 ألف محرف Markdown و153 ألف محرف HTML و357 رابطًا في النتيجة العادية. تجربة نقر عناصر التبويب والتفاصيل أضافت قدرًا محدودًا من HTML وMarkdown، ما يؤكد أن الآلية مفيدة للصفحات التفاعلية لكنها لا تعني تلقائيًا أن كل محتوى مخفي أو كل فيديو صار صالحًا للنشر. خيار الاحتفاظ الصفري غير متاح للحساب الحالي؛ لم تُرسل بيانات شخصية أو تُكتب نتائج إلى قاعدة البيانات.
