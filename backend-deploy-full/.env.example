# Backend Configuration
MONGODB_URI=mongodb+srv://...
PORT=20032
NODE_ENV=production
FRONTEND_URL=https://crossfire.wiki
ADMIN_PASSWORD=change-to-secure-password
JWT_SECRET=generate-a-very-long-random-secret-here

# ASSETS STRATEGY (choose one):
# 1. Database URLs (default, recommended for low-disk): assets stored as URLs in MongoDB, no local files needed
# 2. Local files (optional): set ATTACHED_ASSETS_PATH to serve files from disk
# ATTACHED_ASSETS_PATH=/path/to/attached_assets

# Optional: API key for automated scraping endpoints
SCRAPER_API_KEY=optional-for-automation