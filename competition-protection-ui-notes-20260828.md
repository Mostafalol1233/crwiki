# ملاحظات فحص حماية المسابقة — 2026-08-28

تم فتح صفحة `/ar/competition?competition_test=1` على Preview الخاص بـPR #46. صفحة المسابقة حملت باللغة العربية، وظهر وضع المعاينة الخاصة، وبقيت بوابة التسجيل والنتائج منفصلتين عن مساحة الامتحان.

لم يتم إدخال رقم هاتف أو إرسال طلب مشاركة حقيقي أثناء الفحص. الحماية الجديدة تُفعّل برمجيًا فقط بعد أن يصبح `attemptId` موجودًا وقبل إرسال النتيجة؛ لذلك لا تمنع التسجيل أو قراءة القواعد أو عرض النتائج.

الحماية المضافة في الالتزام `084ca42` تشمل منع النسخ والقص وتحديد النص والسحب والنقر بالزر الأيمن واختصارات النسخ والحفظ والطباعة وPrintScreen داخل حالة الامتحان. حقول الإجابة النصية تستعيد `user-select: text` حتى يظل المشارك قادرًا على الكتابة. عند فقدان نافذة الامتحان للتركيز تُطمس مساحة الأسئلة وتظهر رسالة عودة، وتوجد علامة مائية خافتة داخل مساحة الامتحان. توجد قاعدة طباعة خاصة بالمسابقة فقط.

هذه إجراءات ردع على مستوى المتصفح وليست ضمانًا تقنيًا ضد تصوير الشاشة بكاميرا أو أدوات نظام التشغيل أو متصفح معدل. لا تؤثر الحماية في بقية صفحات الموقع.
## تحقق داخل المتصفح

أعاد طلب `GET /api/content?type=competition&competition_test=1` من داخل صفحة Preview حالة 200، ووجد إعدادات المسابقة و25 سؤالًا، مع `active=true` و`preview_only=true`. لذلك لا يوجد عطل في endpoint؛ ظهور حالة البوابة أثناء اللقطة كان قبل اكتمال دورة تحميل الواجهة.
