
### اختبار الدروس والفيديوهات — 2026-08-26
- `/ar/tutorials` يعمل ويعرض قائمة فيديوهات متعددة مع صور YouTube، وفلاتر الفئات، وحالة فراغ. في الواجهة العربية تبقى عناوين وأوصاف وأزرار كثيرة بالإنجليزية، وبعض بيانات الفيديو تتضمن رموزًا تعبيرية داخل العنوان؛ هذا عيب جودة/تعريب متوسط وليس عطل تحميل.
- `/ar/videos` يعمل ويعرض الفيديو المميز والتصنيفات الأربع والروابط إلى صفحات الفيديو. الـ iframe الخاص بـ YouTube يُحمّل داخل الصفحة، لكن النصوص الأساسية في الصفحة إنجليزية على مسار `/ar`، وهو عدم اتساق مع تجربة اللغة العربية. لم تُنفّذ أي عملية كتابة أو تشغيل متعمد للفيديو.

### اختبار المنتدى — 2026-08-26
- `/ar/forum` يعمل ويعرض ست فئات مع الإحصاءات وروابط النقاش والمساعد، وحالة الخطأ والفراغ موجودتان في الكود. المحتوى العربي في العناوين والشرح جيد نسبيًا، مع بقاء بعض النصوص الإنجليزية في الإحصاءات/الأسماء.
- `/ar/forum/general` يعمل ويجلب ستة موضوعات مع الردود والمشاهدات والنشاط وروابط التفاصيل. زر إنشاء موضوع موجود، لكن لم يُنفّذ إرسال أو كتابة. المسار العام محمي عمليًا لاحقًا في صفحة إنشاء الموضوع، ويجب إبقاء هذا القيد عند أي إصلاح.

### اختبار تفاصيل المنتدى والمراجعات — 2026-08-26
- تفاصيل الموضوع `/ar/forum/general/<id>` تعمل وتعرض الموضوع والردود والصور ونموذج الرد. النموذج يسمح باسم وردّ للزائر غير المسجل، لذلك يجب أن تبقى حماية endpoint ومكافحة السبام والتحقق من الملكية مطبقة خادميًا؛ لم أرسل ردًا.
- `/ar/reviews` تعمل وتعرض ستة بائعين، لكن النص الرئيسي والأزرار الإنجليزية على المسار العربي. بائع بلا مراجعات يظهر بتقييم `0.0` ونجوم فارغة، وهذا يحتاج تسمية صريحة مثل «لا توجد مراجعات» بدل تقديمه كتقييم. ظهرت صورة متجر ضياء مرتين داخل البطاقة، وهو خلل عرض/بيانات.
- الكود يؤكد أن `/reviews/seller/:sellerName` يبحث محليًا فقط، وأن جلب `sellerPage` معطّل دائمًا (`enabled: false`)، والتحقق بالفيديو يعيد `reviewVerificationEnabled: false` بشكل ثابت. أزرار إدارة معرض/تفاصيل البائع تعرض رسالة «يتطلب backend» ولا تحفظ شيئًا؛ هذه وظائف stub يجب إما ربطها endpoint موثق أو إخفاؤها تمامًا.
- إرسال المراجعة يعتمد على `userToken` في localStorage قبل استدعاء طبقة البيانات؛ يلزم تأكيد الهوية والصلاحية خادميًا وعدم الثقة بقيم localStorage أو userId القادم من المتصفح.

### اختبار التواصل والتنزيل — 2026-08-26
- `/ar/contact` تعمل وتعرض بريد التواصل وروابط واتساب/Discord/Facebook/X ونموذج رسالة. النص العربي جيد في المقدمة، مع بقاء بعض العناوين العامة بالإنجليزية في عناصر مشتركة. لم أرسل النموذج؛ يلزم استمرار التحقق الخادمي ومكافحة السبام في `/api/tickets`.
- `/ar/download` تعمل وتعرض رابطَي العميل وملاحظات عدم الانتماء الرسمية ومتطلبات التشغيل وخطوات التثبيت. هي صفحة ثابتة منخفضة المخاطر، لكن معظم العناوين الرئيسية إنجليزية على المسار العربي ومتطلبات النظام تحتاج مراجعة دورية من المصدر الرسمي بدل اعتبارها حقيقة دائمة.

### اختبار مركز المحتوى — 2026-08-26
- `/ar/content-hub/weapon-guides` يعمل بقالب مفصل موحد، ويعرض صورة رئيسية وأقسامًا وروابط إلى الأسلحة والمقارنة والصفحات المنشورة.
- `/ar/content-hub/game-modes` لم يُظهر صفحة تفاصيل للمود كما يوحي الـslug؛ أعاد صفحة مركز المحتوى الرئيسية بعنوان «اكتشف اللعبة من الداخل» وبطاقات الأقسام الستة. هذا يدل على عدم اتساق route/data mapping: الرابط المطلوب من المسار لا يطابق slug مسجلًا، ويجب إما إصلاح slug أو إرجاع 404/صفحة التفاصيل الصحيحة بدل إعادة الفهرس بصمت.
- الكود يؤكد أن مركز المحتوى ثابت بالكامل من `globalContentPages` ولا يجلب بيانات أو تحققًا ديناميكيًا؛ مناسب كصفحة تحريرية، لكنه لا يحقق وحده مطلب تحديث الويكي مع المحتوى الجديد أو ضمان صحة المصادر.

اختبار إضافي لمركز المحتوى: `/ar/content-hub/regional-overviews` و`/ar/content-hub/mode-and-map-collections` يعملان كصفحات تفاصيل كاملة بصور وأقسام وروابط. الخلل السابق في `/ar/content-hub/game-modes` هو رابط slug غير مسجل يعيد صفحة الفهرس؛ يجب إصلاح الرابط/الـslug بدل ترك سلوك صامت مضلل.

### اختبار endpoints الإدارية والكشط في Production — 2026-08-26
أعادت `/api/admin/rebuild` و`/api/admin/scraper` الحالة `401` دون توثيق، وهو سلوك صحيح مبدئيًا. في المقابل أعاد `/api/scrape/forum-list` الحالة `200` دون توثيق وأرجع بيانات منتدى، ما يؤكد أن scraper العام كان مفتوحًا. كما أعادت `/api/scrape/multiple-events` و`/api/scrape/validate-content` الحالة `404`، وأعاد `/api/admin/scrape-and-create-events` صفحة Vercel غير موجودة؛ هذه هي أسباب مباشرة لأزرار لوحة Scraper التي تستدعي مسارات غير منشورة. لم تُنفّذ أي كتابة أو طلب إداري موثق.

### اختبار الدعم والحساب — 2026-08-26
صفحة `/ar/support` تعمل وتعرض نموذج تذكرة كاملًا بالعربية، لكن بعض عناصر الخيارات والنصوص الداخلية ما زالت بالإنجليزية، ولم يُرسل أي نموذج. صفحة `/ar/my-tickets` لا تطلب جلسة دخول؛ تعرض حقل بريد وزر «عرض تذاكري» للبحث عن التذاكر. هذا ليس تسريبًا مؤكدًا في الاختبار السلبي، لكنه نموذج وصول حساس يجب أن يعتمد على رمز وصول أحادي الاستخدام أو جلسة المستخدم، لا على البريد وحده، وإلا يمكن تخمين بريد شخص آخر وعرض تذاكره إذا كان endpoint الخلفي يثق بالبريد فقط.

اختبار إضافي للحساب والمساعد: `/ar/profile` يحجب البيانات ويعرض بوابة تسجيل الدخول عند غياب الجلسة، لكن العناوين والأزرار الأساسية تظهر بالإنجليزية في المسار العربي. `/ar/ai` يعرض واجهة المساعد والاقتراحات العربية دون كشف مفتاح في الصفحة؛ لم تُرسل رسالة أثناء التدقيق لتجنب استهلاك/تغيير أي حالة.
