# تقرير المراجعة الشاملة لمشروع CrossFire Wiki

## الملخص التنفيذي

أُنجزت مراجعة عملية للمستودع والواجهة العامة وطبقة المصادقة وواجهات API وطبقة البيانات ولوحة الإدارة ومسارات الكشط، ثم نُفذت إصلاحات عالية الأولوية على فرع مستقل. لم يتغير فرع `main` ولم يُنفذ نشر Production جديد. النسخة التي اختُبرت على Vercel Preview أصبحت جاهزة للمراجعة على الرابط التالي: [Preview الخاص بفرع المراجعة](https://crossfirewiki-o6bc4jk2i-mostafalol1233s-projects.vercel.app).

النتيجة الأهم هي أن صفحة المنشورات لم تعد فارغة. قبل الإصلاح كان `/api/content?type=posts` يعيد `posts=[]` و`total=0`، بينما يعيد Preview بعد الإصلاح خمسة منشورات من إجمالي سبعة مع ظهور البطاقات والصور والروابط في `/ar/posts`. السبب التشغيلي المؤكد في الكود هو أن endpoint كان يستخدم إسقاطًا واحدًا واسعًا لحقول كثيرة، وأي عمود غير موجود أو خطأ استعلام كان يتحول إلى استجابة فارغة بلا تمييز؛ أضيفت إسقاطات احتياطية تدريجية، فأثبت Preview أن المسار يعمل مع المخطط الحالي [1] [2]. لا يمكن الجزم من دون قراءة سياسات وقاعدة Supabase مباشرة بأن أي منشورات لم تُحذف سابقًا، لذلك لم تُنفذ إعادة تعبئة أو بيانات مصطنعة.

> الخلاصة المهنية: فرع Preview الحالي أفضل وأأمن من Production السابق من ناحية المسارات التي عولجت، لكنه ليس جاهزًا للدمج في `main` قبل ضبط سر الإدارة المستقل، والتحقق المباشر من سياسات قاعدة البيانات، واتخاذ قرار واضح بشأن صفحة التذاكر والترجمة وجودة وظائف المراجعات.

## نطاق المراجعة وخط الأساس

شملت المراجعة بنية React وVite وWouter ومسارات اللغة العربية، ملفات API الاثنتي عشرة، طبقة Supabase والشيم، المصادقة العامة والإدارية، الكشط، البريد، الإدارة، الصفحات العامة، مركز المحتوى، الحساب، المنتدى، المراجعات، الدعم، التنزيل، SEO وملفات SQL المرفقة. تم الحفاظ على سقف Vercel الصارم؛ عدد ملفات الدوال بقي **12** بعد الإصلاحات.

| العنصر | النتيجة |
|---|---|
| فرع Production قبل المراجعة | `main` عند الالتزام `1e37dbc` |
| فرع الإصلاحات | `audit/hardening-20260826` |
| آخر commit للإصلاحات | `0044fb5`، بعد `ddff0ab` و`bcccf7b` و`e8ce061` |
| آخر commit للتوثيق والتحقق | `8401810` |
| عدد دوال Vercel | 12، دون إضافة دالة جديدة |
| فحص TypeScript | ناجح |
| البناء الكامل | ناجح، مع تحذير قائم من أحجام حزم كبيرة |
| الاختبار الخفيف الجديد | ناجح: `audit-smoke: passed` |
| Preview الكود | READY، commit `0044fb5` |
| Preview النهائي بعد التوثيق | READY، commit `8401810` |
| نشر Production | لم يتغير أثناء هذه المراجعة |

## الإصلاحات المنفذة

### إصلاح المنشورات وتضارب مخطط البيانات

أُعيدت كتابة قراءة المنشورات في `api/sitemap.ts` لتجربة إسقاط موسع ثم إسقاطات مستقرة وأخيرًا إسقاط حد أدنى. أصبح فشل عمود اختياري لا يحول الأرشيف كله إلى صفر، مع تسجيل فشل داخلي إذا فشلت جميع المحاولات [2]. كما أُزيلت بيانات fallback التي كانت تعرض أسلحة وخرائط وأحداثًا ومنشورات تجريبية عند تعطل المصدر؛ الآن تظهر حالة فراغ صادقة بدل إدخال معلومات غير موثقة [9].

### إغلاق الكشط العام وربط لوحة الإدارة بالخادم

كان `/api/scrape/forum-list` يعيد بيانات المنتدى بحالة 200 من دون توثيق، وكانت لوحة Scraper تستدعي مسارات غير منشورة مثل `multiple-events` و`validate-content` و`admin/scrape-and-create-events`. أصبحت كل actions الكشط تتطلب توكن إدارة، وأضيفت مسارات `multiple-events` و`validate-content` داخل الدالة الحالية من دون تجاوز سقف 12. أضيف فحص مشترك للنطاقات المسموح بها، HTTPS، بيانات الدخول داخل الرابط، وعناوين DNS الخاصة والمحجوزة لمنع SSRF الأساسي [4] [6].

تُرجع استجابة `multiple-events` الآن الحقول التي تعتمد عليها الواجهة، بما فيها `url` و`content` و`category`، وأُعيد ربط زر النشر ببوابة `admin/rebuild` مع تأكيد صريح قبل الكتابة. أضيفت عملية نشر أحداث دفعي محدودة بخمسين سجلًا، تتحقق من المصدر وتمنع التكرار حسب `source_url`. لم تُنفذ عملية نشر فعلية أثناء المراجعة.

### تقوية بوابة الإدارة والبريد

أصبح `ADMIN_TOKEN_SECRET` سرًا مستقلًا إلزاميًا بطول لا يقل عن 32 محرفًا؛ لم يعد توقيع توكن الإدارة يعتمد على كلمة المرور أو مفتاح خدمة Supabase [3]. أضيفت حماية محاولات دخول متكررة في الذاكرة مع رد 429 بعد عدد محاولات فاشلة ضمن نافذة زمنية، مع إبقاء رسائل الرفض عامة.

كان `/api/send-email` مرحّل بريد عامًا يقبل المستلمين وHTML من أي POST. أصبح يتطلب توكن إدارة وصلاحية إرسال، ويحد عدد المستلمين إلى 500، ويتحقق من عناوين البريد وحجم الرسالة ويرفض `script` و`javascript:`، كما عُدلت لوحة البريد لتمرير توكن الإدارة [7].

### إزالة الكتابة المباشرة من المتصفح

أُعيد تنظيم `queryClient` بحيث تمرر دوال Vercel الحقيقية مثل الإدارة والكشط والتسجيل والمساعد والبريد مباشرة إلى الخادم، بدل أن يبتلعها shim محلي قد يعطي نجاحًا شكليًا. كذلك تمرر mutations الخاصة بالأسلحة والأحداث والأخبار والمنشورات والمودات والرتب والمرتزقة والدروس والبائعين إلى `admin-table` الخادمي مع صلاحيات مستقلة، وتطبيع أسماء الحقول بين الواجهة ومخطط قاعدة البيانات [8]. أضيفت عملية ترتيب أحداث محمية، وأصبحت جلسة Supabase ذات أولوية لمسارات المجتمع حتى لا يتعارض حساب المدير مع حسابه العادي. كما نُقلت إدارة الإعلانات إلى مسار خادمي محدود، وصار تحديث إعدادات الموقع يرسل فقط الأعمدة الموجودة في المخطط الفعلي.

### إصلاح مسارات مركز المحتوى

أضيف alias للرابط القديم `game-modes` ليعرض صفحة «مجموعات المودات والخرائط» الصحيحة. أي slug غير معروف يعرض الآن حالة 404 واضحة بدل إعادة فهرس مركز المحتوى بصمت [10]. كما أصبح `/api/content` يعيد 400 صريحًا عند طلب نوع غير مدعوم مثل `news`، بدل إعادة XML sitemap بحالة 200 مضللة؛ صفحات الأخبار الحالية تستخدم طبقة بيانات أخرى.

### حماية المجتمع ولوحة الإدارة والمحادثات

أصبحت التذاكر والردود والمنتدى والمراجعات والتعليقات والإعجابات تمر عبر بوابة مجتمع خادمية تتحقق من جلسة Supabase، وتتحقق من ملكية التذكرة قبل قراءة الردود أو إضافتها، وتطبق حدودًا بسيطة لمعدل الطلبات. أُزيل الاعتماد على البريد أو الاسم أو المعرّف الذي يرسله المتصفح للوصول إلى بيانات التذاكر، وأصبحت صفحة `my-tickets` محمية بتسجيل الدخول مع إزالة فرع البحث بالبريد. كما أزيلت الكتابة المباشرة من شاشة حذف تعليقات الأحداث ومن شاشتي إدارة المراجعات والتذاكر، واستُبدل إعجاب تفاصيل الدروس بعملية المجتمع الموثقة.

نُقلت محادثات Chat إلى البوابة نفسها مع التحقق من العضوية وتحديث دوري بدل قناة بث عامة، وأضيفت لها حالة خطأ وإعادة محاولة. وأصبح `supabaseService` في المتصفح محولًا توافقياً إلى `admin-table` الموثق، وليس عميل service-role؛ لذلك تمر شاشات الإدارة القديمة أيضًا عبر توكن الإدارة، مع دعم القراءة والتعديل والحذف وupsert للموارد الإدارية المحددة. عولجت شاشة SiteSettings لتتعامل مع مخطط الصف الواحد، ونُقلت AdminAnnouncements إلى حفظ خادمي، وقُصرت قراءة المراجعات على الحقول العامة. أضيفت migration RLS قابلة للمراجعة تشمل التذاكر والرسائل والمحادثات والإعلانات، لكنها لم تُنفذ تلقائيًا.

### تقوية المساعد الذكي ومنع المحتوى المصطنع

فُرضت نماذج مجانية افتراضيًا في AI endpoint، وأزيل fallback مفتاح `VITE_*`، وأضيف حد لحجم المحادثة ومعدل الطلبات وفحص أصل الطلب. كما عُطلت سكربتات seed القديمة التي كانت تحتوي محادثات وأسماء افتراضية وادعاءات غير موثقة، حتى لا يعاد إدخال محتوى مصطنع إلى المنتدى. وسكربتات الاستيراد الخادمية تطلب الآن متغيرات خدمة خادمية صريحة بدل أسماء قد تُضمّن في الحزمة.

### الاختبارات

أضيف `scripts/audit-smoke.ts` وأمر `pnpm run audit:smoke`. يغطي الاختبار مطابقة الاسم الظاهر للرتبة مع tier، نسبة 99.9% للمثال الحدّي السابق، انتهاء التوكن وتوقيعه، ورفض HTTP والمصادر غير المسموح بها والمضيف المحلي. نجح الاختبار بعد كل تغييرات الإصلاح.

## نتائج التحقق على Vercel Preview

| الاختبار | النتيجة |
|---|---:|
| الصفحة الرئيسية `/ar/` | 200 |
| صفحة المنشورات `/ar/posts` | 200، وتعرض 7 بطاقات |
| alias مركز المحتوى `/ar/content-hub/game-modes` | 200، ويعرض صفحة المودات والخرائط الصحيحة |
| `/api/content?type=posts&limit=5&offset=0` | 200، خمسة سجلات من إجمالي 7 |
| `/api/content?type=weapons&limit=1&offset=0` | 200، سجل واحد من إجمالي 3599 |
| `/api/content?type=news` | 400 واضح لنوع غير مدعوم |
| `/api/admin/rebuild` دون توثيق | 401 |
| `/api/admin/scraper` دون توثيق | 401 |
| `/api/scrape/forum-list` دون توثيق | 401 |
| `/api/scrape/multiple-events` دون توثيق | 401 |
| `/api/scrape/validate-content` دون توثيق | 401 |
| `/api/send-email` دون توثيق | 401 |
| عدد دوال Vercel | 12 |

لم تُرسل مراجعة أو تعليقًا أو تذكرة، ولم تُستخدم بيانات حساب حقيقي، ولم يُرسل توكن إداري صالح، ولم تُنفذ أي كتابة في قاعدة البيانات. اختبارات الصفحات التفاعلية اقتصرت على العرض والحالات السلبية الآمنة.

## مصفوفة المخاطر والأعمال المتبقية

| الأولوية | المشكلة | الحالة | الإجراء المطلوب |
|---|---|---|---|
| حرجة | ملفات بيئة كانت متعقبة في مستودع GitHub عام | أُزيلت من الفرع الجديد، لكن التاريخ السابق لم يُمحَ | تدوير كل الأسرار القديمة من Vercel وSupabase والمزودين، ثم التأكد من ضبط البدائل الجديدة. إزالة الملف من Git لا تلغي قيمه من تاريخ Git |
| حرجة | `ADMIN_TOKEN_SECRET` أصبح إلزاميًا | أُصلح في الكود ولم يُختبر دخول صالح على Preview | ضبط سر مستقل طويل في بيئات Preview وProduction قبل الدمج، ثم اختبار تسجيل الدخول الإداري مرة واحدة يدويًا. إذا لم يُضبط فالتوكنات لن تُنشأ أو تُقبل |
| حرجة | سياسات RLS في SQL المرفق تسمح بكتابة عامة على جداول حساسة | لم تُفترض كحقيقة مطبقة ولم تُنفذ migration | فحص `pg_policies` مباشرة في Supabase، ثم اعتماد سياسات ملكية باستخدام `auth.uid()` وترك الإدارة للبوابة الخادمية بمفتاح الخدمة. لا تُشغّل SQL المرفق كما هو |
| عالية | صفحة `my-tickets` تبحث بالبريد فقط | أُصلحت على فرع Preview بجلسة Supabase، وظهرت بوابة دخول بلا حقل بريد في smoke test | اختبارها بحساب تجريبي على Preview، ثم اعتماد migration RLS الحية |
| عالية | أجزاء من shim ومسارات الإدارة القديمة كانت تنفذ CRUD مباشرًا | أُصلحت دفعة `ddff0ab`؛ المحول الإداري يمر عبر admin-table، والمجتمع عبر بوابة جلسة | اختبار أدوار الإدارة فعليًا على Preview وفحص أي مورد جديد يضاف لاحقًا |
| عالية | صلاحيات لوحة الإدارة تحتاج اختبارًا بحسابات أدوار فعلية | حماية المصدر موجودة، لكن لم يُستخدم توكن صالح في التدقيق | اختبار super admin ومدير محتوى ومدير خدمات على Preview ببيانات لا تكشف أسرارًا، والتأكد من أن كل زر يعرض فشل الخادم بدل نجاحًا وهميًا |
| متوسطة | العربية غير مكتملة في الدروس والفيديوهات والملف الشخصي والمراجعات والتنزيل والدعم | عولجت العناوين والحالات الأساسية في الدفعة الحالية باستخدام الحقول العربية الموجودة، مع بقاء fallback إنجليزي للبيانات غير المترجمة | استكمال الترجمة المصدرية ومراجعة النصوص والرموز غير المرغوبة يدويًا |
| متوسطة | المراجعات تكرر معرض الصور، والتقييم صفر للبائع بلا مراجعات، والتحقق بالفيديو stub | عولجت القراءة الآمنة، حالة التقييم، معرض البائع، والتحقق الخادمي في Preview؛ لم يُختبر إرسال مراجعة بحساب حقيقي | اختبار تدفق مراجعة واحد بحساب تجريبي بعد موافقة المستخدم، ثم مراجعة إعدادات الفيديو |
| متوسطة | إعدادات custom domain | لم تتغير | ربط `crossfire.wiki` بمشروع Vercel الحالي من لوحة Domains أو DNS ثم التحقق من أن بصمة الأصول تطابق آخر نشر؛ النطاق كان يخدم حزمة أقدم مختلفة |
| منخفضة | أحجام حزم العميل كبيرة | تحذير البناء قائم، لا يمنع النشر | تقسيم حزم Dashboard وTipTap وواجهة العميل أكثر، وتحميل المحرر عند الحاجة فقط |

## قاعدة البيانات والمحتوى وSEO

لم تُشغّل ملفات SQL المرفقة تلقائيًا، ولم تُنفذ إعادة بناء أو إعادة scraping جماعية، لأن ذلك قد يحذف محتوى قائمًا أو يخلط نسخ CrossFire الإقليمية. يجب التعامل مع كل مصدر على مستوى المنطقة والتاريخ، وتسجيل `source_url` وحالة التحقق قبل نشر أي معلومة. وجود سبعة منشورات في Preview يثبت سلامة القراءة الحالية، لكنه لا يثبت أن عدد المحتوى السابق كان كاملًا أو أن أي سجلات مفقودة يمكن استعادتها بلا نسخة احتياطية.

بنية sitemap الحالية تجمع الأحداث والأخبار والمنشورات والدروس والصفحات والأسلحة والمرتزقة والمناطق، مع إبقاء المسابقة خارج sitemap ما لم تكن فعالة. لم تُغيّر إصلاحات المراجعة بوابة المسابقة أو صلاحية Preview أو قواعد sitemap الخاصة بها. تظل فهرسة كل منشور أو حدث جديد معتمدة على أن السجل المنشور يحمل slug ومصدرًا ووصفًا وصورة صحيحة، وعلى أن النطاق المخصص يشير إلى المشروع الصحيح.

## قرار النشر

الفرع `audit/hardening-20260826` مرفوع إلى GitHub. Preview الكود عند `0044fb5` وPreview التوثيق النهائي عند `8401810` كلاهما READY، والاختبارات المحلية وHTTP والحالات السلبية في المتصفح ناجحة. أوصي بعدم الدمج في `main` أو إعادة نشر Production قبل تنفيذ ثلاثة بنود: ضبط `ADMIN_TOKEN_SECRET` المستقل واختبار دخول إداري صالح، فحص سياسات RLS الحية واعتماد migration المرفقة بعد مراجعتها، ثم اختبار أدوار الإدارة وحالات إنشاء تذكرة وتعليق ومحادثة ومراجعة بحساب تجريبي. بعد ذلك يمكن فتح طلب دمج ومراجعة Preview مرة أخيرة.

## المراجع

[1]: https://github.com/Mostafalol1233/crwiki/tree/audit/hardening-20260826 — فرع إصلاحات المراجعة في GitHub.

[2]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/api/sitemap.ts — endpoint المحتوى وsitemap وإسقاطات posts.

[3]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/server/adminAuth.ts — توقيع والتحقق من توكن الإدارة.

[4]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/server/urlSafety.ts — تحقق المصادر العامة وفحص DNS.

[5]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/api/admin/rebuild.ts — بوابة عمليات الإدارة والنشر الدفعي.

[6]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/api/scrape/%5Baction%5D.ts — actions الكشط والتحقق.

[7]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/api/send-email.ts — endpoint البريد بعد التقييد.

[8]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/client/src/lib/queryClient.ts — طبقة توجيه طلبات العميل.

[9]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/client/src/lib/supabaseApi.ts — طبقة البيانات وfallback الصادق.

[10]: https://github.com/Mostafalol1233/crwiki/blob/0044fb57e2bf393c1d744d696a7cc80f27e096fb/client/src/pages/GlobalContentHub.tsx — route mapping لمركز المحتوى.

[11]: https://crossfirewiki-nluawjmul-mostafalol1233s-projects.vercel.app — Preview النهائي بعد توثيق التحقق.

[13]: https://crossfirewiki-o6bc4jk2i-mostafalol1233s-projects.vercel.app — Preview الكود عند `0044fb5`.

[12]: https://crossfire.z8games.com/ranks.html — المصدر المشار إليه في الكود لعتبات رتب CrossFire.
