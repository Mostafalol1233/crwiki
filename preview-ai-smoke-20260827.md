# فحص المساعد الذكي — 2026-08-27

## السبب

كان مسار `/api/ai/chat` يعمل، لكن قائمة النماذج المجانية القديمة لم تعد متوافقة مع حالة المزود الحالية. النموذج الأول أعاد رفضًا عامًا من المزود، ونموذج NVIDIA احتاج إعدادات خصوصية وبيانات غير متاحة، بينما نموذج OpenAI الاحتياطي لم يعد متاحًا مجانًا. لذلك أعاد endpoint حالة HTTP 503 بعد فشل جميع البدائل.

## الإصلاح

تم تحديث البديل الافتراضي إلى `minimax/minimax-m3:free`، مع بديلين مجانيين حاليين هما `google/gemma-4-26b-a4b-it:free` و`z-ai/glm-5.2:free`. تم تقليل مهلة المحاولة، وإضافة رسالة خطأ عامة لا تكشف تفاصيل المزود للمستخدم، وتسجيل السبب في سجل الخادم فقط. كما أُضيف منع للرموز التعبيرية في ردود المساعد.

## النتائج

| البيئة | المسار | النتيجة |
|---|---|---:|
| Preview الالتزام `898c634` | `/api/ai/chat` | 200 |
| Production الالتزام `26e00af` | `/api/ai/chat` عبر رابط النشر المباشر | 200 |
| Production عبر `crossfire.wiki` | `/api/ai/chat` | 503 لأن النطاق ما زال مرتبطًا بنشر أقدم |

تم اختبار سؤال عام عن الفرق بين GP وZP. الرد في Preview وProduction المباشر وصل بصيغة بث صحيحة ومن دون رموز تعبيرية.

لم يتم تعديل أي مفتاح أو متغير بيئة أو إعداد Supabase. بعد ربط `crossfire.wiki` بنشر Production الجديد سيستخدم النطاق العام الإصلاح نفسه.
